import QueryBuilder from 'lib/query-builder';
import {QueryResult} from 'pg';

export async function up(builder: QueryBuilder, query: (queryText: string) => Promise<QueryResult>) {

}

export async function down(builder: QueryBuilder, query: (queryText: string) => Promise<QueryResult>) {

}

export async function change(builder: QueryBuilder, query: (queryText: string) => Promise<QueryResult>) {

}